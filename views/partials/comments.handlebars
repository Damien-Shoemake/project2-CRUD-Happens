<div class='comments'>
  {{#each this}}
    <section class='comment'>
      <div class='meta'>
        {{user.name}}
        on
        {{format_date created_at}}
      </div>
      <div class='text'>
        {{comment}}
      </div>
    </section>
  {{/each}}
</div>